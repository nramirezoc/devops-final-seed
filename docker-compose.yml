version: '3.9'

services:
  todo-api:
    build: .
    container_name: todo-api-container
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - DB_PATH=tasks.db
    restart: always